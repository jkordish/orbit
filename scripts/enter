#!/bin/bash
# Inspect one project without evaluating its code or changing machine state.
set -euo pipefail
ORBIT_ROOT=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)
exec python3 "$ORBIT_ROOT/scripts/enter.py" "$@"
